namespace ModMenu.Config {
    /**
     * <summary>
     * The types of fields which mod menu can display.
     * </summary>
     */
    public enum FieldType {
        None,
        Toggle,
        Color,
        KeyCode,
        Slider,
        Text,
        Dropdown,
        ReadOnly,
    }
}
